package ejercicios;

import java.io.IOException;

public class Excepciones {

	public static void main(String[] args) throws IOException {
	/*
	 	// Excepcion de string, no se puede convertir una frase a uppercase y tira error
		String s = null;
		s.toUpperCase();
		// No se puede dividir entre 0, intenta la division y si no me da el valor maximo
		int div, a = 5, b = 0;
		try {
			div = a / b;
		} catch(ArithmeticException ae) {
			div = Integer.MAX_VALUE;
		}
		// System.in.read() al ser de In-Out Exception (IOException) es OBLIGATORIO filtrar el error
		char c = (char)System.in.read();
		
		try {
			char c = (char)System.in.read();
		} catch(IOException e) {
			syso "NO se a podido leer de la consola
		}
		
		
		
		
	 */
	}

}
